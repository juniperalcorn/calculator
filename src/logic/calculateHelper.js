function checkButtonAC(buttonName) {
    if (buttonName === "AC") {
      return {
        total: null,
        next: null,
        operation: null,
      };
    }
}

module.exports = {checkButtonAC}
